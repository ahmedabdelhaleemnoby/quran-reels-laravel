# تثبيت FFmpeg على Windows

## ⚠️ ملاحظة مهمة
**FFmpeg على Windows لا يُثبت من الترمنال** - يحتاج تحميل يدوي.

---

## 📥 الخطوة 1: تحميل FFmpeg

### **الطريقة 1: التحميل المباشر (موصى به)**

1. **اذهب إلى:**
   https://www.gyan.dev/ffmpeg/builds/

2. **حمّل:**
   - اضغط على: **"ffmpeg-release-essentials.zip"**
   - أو: **"ffmpeg-git-essentials.7z"**

3. **الحجم:** ~75 MB تقريباً

---

## 📂 الخطوة 2: فك الضغط

1. **بعد التحميل:**
   - فك ضغط الملف
   - ستجد مجلد اسمه: `ffmpeg-2024-xx-xx-git-xxxxx-essentials_build`

2. **انقل المجلد:**
   - انقله إلى: `C:\ffmpeg`
   - (أو أي مكان تفضله، لكن `C:\ffmpeg` أسهل)

3. **التأكد:**
   - يجب أن يكون المسار: `C:\ffmpeg\bin\ffmpeg.exe`

---

## ⚙️ الخطوة 3: إضافة FFmpeg للـ PATH

### **الطريقة السهلة:**

1. **افتح PowerShell كـ Administrator:**
   - اضغط `Win + X`
   - اختر "Windows PowerShell (Admin)" أو "Terminal (Admin)"

2. **نفذ هذا الأمر:**
   ```powershell
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\ffmpeg\bin", "Machine")
   ```

3. **أعد تشغيل PowerShell**

---

### **الطريقة اليدوية (إذا لم تنجح الأولى):**

1. **افتح System Properties:**
   - اضغط `Win + R`
   - اكتب: `sysdm.cpl`
   - اضغط Enter

2. **اذهب إلى Environment Variables:**
   - اضغط على تاب "Advanced"
   - اضغط "Environment Variables"

3. **عدّل Path:**
   - في "System variables" (الجزء السفلي)
   - ابحث عن "Path"
   - اضغط "Edit"
   - اضغط "New"
   - أضف: `C:\ffmpeg\bin`
   - اضغط OK على كل النوافذ

4. **أعد تشغيل PowerShell**

---

## ✅ الخطوة 4: التحقق من التثبيت

1. **افتح PowerShell جديد**

2. **نفذ:**
   ```powershell
   ffmpeg -version
   ```

3. **يجب أن ترى:**
   ```
   ffmpeg version N-xxxxx-gxxxxxxx
   built with gcc x.x.x
   ...
   ```

---

## 🎯 الخطوة 5: تشغيل المشروع

بعد تثبيت FFmpeg:

```powershell
cd C:\Users\IP\Downloads\quran-reels-laravel-main\quran-reels-laravel-main
php artisan serve
```

افتح المتصفح:
```
http://localhost:8000
```

---

## 🐛 حل المشاكل

### **المشكلة 1: "ffmpeg is not recognized"**

**الحل:**
- تأكد أن المسار صحيح: `C:\ffmpeg\bin\ffmpeg.exe`
- أعد تشغيل PowerShell بعد إضافة PATH
- جرب فتح PowerShell جديد كـ Administrator

### **المشكلة 2: "Access Denied"**

**الحل:**
- افتح PowerShell كـ Administrator
- أعد تنفيذ أمر إضافة PATH

### **المشكلة 3: لا يزال لا يعمل**

**الحل المؤقت:**
استخدم المسار الكامل في الكود:
```php
// في ملف QuranVideoService.php
$ffmpegPath = 'C:\\ffmpeg\\bin\\ffmpeg.exe';
```

---

## 📊 الخلاصة

| الخطوة | الوقت | الصعوبة |
|--------|-------|---------|
| **التحميل** | 2 دقيقة | سهل |
| **فك الضغط** | 1 دقيقة | سهل |
| **إضافة PATH** | 2 دقيقة | متوسط |
| **التحقق** | 1 دقيقة | سهل |
| **المجموع** | **~5 دقائق** | ⭐⭐ |

---

## 🚀 بعد التثبيت

**للوكال:**
- ✅ FFmpeg مثبت
- ✅ المشروع يعمل
- ✅ يمكنك توليد فيديوهات

**للإنتاج:**
- 🎯 انتقل لـ VPS (راجع `VPS_COMPLETE_GUIDE.md`)
- 💰 $6/شهر فقط
- 🌐 متاح للجميع على الإنترنت

---

## 💡 نصيحة

**FFmpeg على Windows للتطوير فقط.**

للإنتاج (Production):
- استخدم VPS
- أسرع وأفضل
- لا مشاكل في PATH أو الصلاحيات
